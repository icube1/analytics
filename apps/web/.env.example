# Vite SPA API contract
# Leave empty for same-origin /api (recommended behind nginx reverse proxy).
# VITE_API_BASE=https://gala-soft.ru

# Dev-only: where Vite proxies /api requests (Next.js or Axum backend).
VITE_API_PROXY_TARGET=http://127.0.0.1:3000
