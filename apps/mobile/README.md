# @analytics/mobile

Capacitor shell for the Vite SPA. See [docs/mobile-capacitor.md](../../docs/mobile-capacitor.md).

```bash
npm run build:mobile    # from repo root
cd apps/mobile && npm run cap:sync
```
