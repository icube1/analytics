[package]
name = "finance-wasm"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "wasm-bindgen adapter for finance-core batch evaluation"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
finance-core = { path = "../finance-core" }
serde_json = "1.0.151"
wasm-bindgen = "0.2.108"

[lints]
workspace = true
